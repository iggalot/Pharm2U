<UserControl x:Class="Pharm2U.Views.OrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pharm2U.Views"
             xmlns:controls="clr-namespace:Pharm2U.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    
    <Grid>
        <Border Padding="5" Background="White">
            <StackPanel Orientation="Vertical" Background="Aquamarine">
                <!-- The Main List -->
                <ListBox Name="ICOrderList" DataContext="{Binding OrderData}" ItemsSource="{Binding Data}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <controls:OrderListItemControl />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

        </Border>

    </Grid>
</UserControl>
